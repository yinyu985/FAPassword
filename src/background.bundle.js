// Generated by npm run bundle:background from src/background.js. Do not edit.
(() => {
  // src/crypto.js
  function hexToBytes(hex) {
    if (typeof hex !== "string" || !/^(?:0x)?[0-9a-f]*$/i.test(hex)) throw new Error("invalid hex encoding");
    if (/^0x/i.test(hex)) hex = hex.slice(2);
    if (hex.length % 2) hex = "0" + hex;
    const out = new Uint8Array(hex.length / 2);
    for (let i = 0; i < out.length; i++) out[i] = parseInt(hex.slice(i * 2, i * 2 + 2), 16);
    return out;
  }
  function bytesToHex(bytes) {
    let s = "";
    for (const b of bytes) s += b.toString(16).padStart(2, "0");
    return s;
  }
  function base64ToBytes(b64) {
    if (typeof b64 !== "string" || !/^(?:[A-Za-z0-9+/]{4})*(?:[A-Za-z0-9+/]{2}==|[A-Za-z0-9+/]{3}=)?$/.test(b64)) {
      throw new Error("invalid base64 encoding");
    }
    const bin = atob(b64);
    if (btoa(bin) !== b64) throw new Error("non-canonical base64 encoding");
    const out = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; i++) out[i] = bin.charCodeAt(i);
    return out;
  }
  function bytesToBase64(bytes) {
    let bin = "";
    for (const b of bytes) bin += String.fromCharCode(b);
    return btoa(bin);
  }
  function utf8ToBytes(str) {
    return new TextEncoder().encode(str);
  }
  function bytesToUtf8(bytes) {
    return new TextDecoder("utf-8", { fatal: true }).decode(bytes);
  }
  function concatBytes(...arrays) {
    let len = 0;
    for (const a of arrays) len += a.length;
    const out = new Uint8Array(len);
    let off = 0;
    for (const a of arrays) {
      out.set(a, off);
      off += a.length;
    }
    return out;
  }
  function bigIntToBytes(n) {
    if (n === 0n) return new Uint8Array([0]);
    const out = [];
    while (n > 0n) {
      out.unshift(Number(n & 0xffn));
      n >>= 8n;
    }
    return new Uint8Array(out);
  }
  function bytesToBigInt(bytes) {
    let n = 0n;
    for (const b of bytes) n = n << 8n | BigInt(b);
    return n;
  }
  function padBytes(bytes, length) {
    if (bytes.length >= length) return bytes.slice(bytes.length - length);
    const out = new Uint8Array(length);
    out.set(bytes, length - bytes.length);
    return out;
  }
  function constantTimeEqual(a, b) {
    if (a.length !== b.length) return false;
    let diff = 0;
    for (let i = 0; i < a.length; i++) diff |= a[i] ^ b[i];
    return diff === 0;
  }
  async function sha256(...inputs) {
    const data = concatBytes(...inputs.map((i) => i instanceof Uint8Array ? i : utf8ToBytes(String(i))));
    return new Uint8Array(await crypto.subtle.digest("SHA-256", data));
  }
  function randomBytes(count) {
    const a = new Uint8Array(count);
    crypto.getRandomValues(a);
    return a;
  }
  function mod(a, n) {
    a %= n;
    if (a < 0n) a += n;
    return a;
  }
  function powmod(base, exp, n) {
    if (exp < 0n) throw new Error("negative exponent unsupported");
    base = mod(base, n);
    let result = 1n;
    while (exp > 0n) {
      if (exp & 1n) result = mod(result * base, n);
      base = mod(base * base, n);
      exp >>= 1n;
    }
    return result;
  }
  var QueryStatus = {
    Success: 0,
    GenericError: 1,
    InvalidParam: 2,
    NoResults: 3,
    FailedToDelete: 4,
    FailedToUpdate: 5,
    InvalidMessageFormat: 6,
    DuplicateItem: 7,
    UnknownAction: 8,
    InvalidSession: 9
  };
  function queryStatusError(status) {
    const map = {
      1: "Generic query error",
      2: "Invalid query param",
      3: "No query results",
      4: "Failed to delete",
      5: "Failed to update",
      6: "Invalid message format",
      7: "Duplicate item",
      8: "Unknown action",
      9: "Invalid session"
    };
    const error = new Error(map[status] ?? `Query error: status ${status}`);
    error.status = status;
    return error;
  }

  // src/srp.js
  var GROUP_PRIME = BigInt(
    ""
  );
  var GROUP_PRIME_BYTES = 3072 >> 3;
  var GROUP_GENERATOR = 5n;
  if (GROUP_PRIME.toString(16).length !== 768) {
    throw new Error("SRP group prime is corrupt (expected 3072 bits / 768 hex digits)");
  }
  var SecretSessionVersion = {
    SRPWithOldVerification: 0,
    SRPWithRFCVerification: 1
  };
  var MSGType = {
    ClientKeyExchange: 0,
    ServerKeyExchange: 1,
    ClientVerification: 2,
    ServerVerification: 3
  };
  var SRPSession = class {
    constructor(shouldUseBase64 = false) {
      this.shouldUseBase64 = !!shouldUseBase64;
      this.usernameBytes = randomBytes(16);
      this.username = this.serialize(this.usernameBytes);
      this.clientPrivateKey = bytesToBigInt(randomBytes(32));
      this.serverPublicKey = void 0;
      this.salt = void 0;
      this.sharedKey = void 0;
      this._encryptionKey = void 0;
    }
    get clientPublicKey() {
      return powmod(GROUP_GENERATOR, this.clientPrivateKey, GROUP_PRIME);
    }
    // A and B keep the helper's own minimal big-endian encoding here (what we put on the
    // wire). only u pads them, per RFC 5054 PAD() - matching the reference client
    get clientPublicKeyBytes() {
      return bigIntToBytes(this.clientPublicKey);
    }
    get serverPublicKeyBytes() {
      return bigIntToBytes(this.serverPublicKey);
    }
    serialize(bytes, prefix = true) {
      if (this.shouldUseBase64) return bytesToBase64(bytes);
      return (prefix ? "0x" : "") + bytesToHex(bytes);
    }
    deserialize(str) {
      if (this.shouldUseBase64) return base64ToBytes(str);
      return hexToBytes(str.replace(/^0x/, ""));
    }
    // salt stays the exact bytes the helper sent. round-tripping it through a bigint drops
    // any leading zero byte, which silently changes x and M and looks like a wrong PIN
    setServerPublicKey(serverPublicKey, saltBytes) {
      if (serverPublicKey <= 0n || serverPublicKey >= GROUP_PRIME)
        throw new Error("invalid server public key: out of range");
      if (mod(serverPublicKey, GROUP_PRIME) === 0n) throw new Error("invalid server public key");
      if (!(saltBytes instanceof Uint8Array) || saltBytes.length === 0) throw new Error("invalid salt");
      this.serverPublicKey = serverPublicKey;
      this.salt = saltBytes;
    }
    async setSharedKey(pin) {
      if (this.serverPublicKey === void 0) throw new Error("missing server public key");
      if (this.salt === void 0) throw new Error("missing salt");
      const A = this.clientPublicKeyBytes;
      const B = this.serverPublicKeyBytes;
      const u = bytesToBigInt(await sha256(padBytes(A, GROUP_PRIME_BYTES), padBytes(B, GROUP_PRIME_BYTES)));
      if (u === 0n) throw new Error("invalid SRP parameter: u == 0");
      const k = bytesToBigInt(
        await sha256(bigIntToBytes(GROUP_PRIME), padBytes(bigIntToBytes(GROUP_GENERATOR), GROUP_PRIME_BYTES))
      );
      const innerHash = await sha256(utf8ToBytes(this.username + ":" + pin));
      const x = bytesToBigInt(await sha256(this.salt, innerHash));
      const base = mod(this.serverPublicKey - mod(k * powmod(GROUP_GENERATOR, x, GROUP_PRIME), GROUP_PRIME), GROUP_PRIME);
      const exp = this.clientPrivateKey + u * x;
      const S = powmod(base, exp, GROUP_PRIME);
      this.sharedKey = bytesToBigInt(await sha256(bigIntToBytes(S)));
      this._encryptionKey = void 0;
    }
    // M = H( H(N) XOR H(g) | H(I) | s | A | B | K )
    async computeM() {
      if (this.sharedKey === void 0) throw new Error("missing shared key");
      const hN = await sha256(bigIntToBytes(GROUP_PRIME));
      const hg = await sha256(padBytes(bigIntToBytes(GROUP_GENERATOR), GROUP_PRIME_BYTES));
      const hI = await sha256(utf8ToBytes(this.username));
      const xored = new Uint8Array(hN.length);
      for (let i = 0; i < hN.length; i++) xored[i] = hN[i] ^ hg[i];
      return await sha256(
        xored,
        hI,
        this.salt,
        this.clientPublicKeyBytes,
        this.serverPublicKeyBytes,
        padBytes(bigIntToBytes(this.sharedKey), 32)
      );
    }
    // HAMK = H( A | M | K )
    async computeHMAC(m) {
      return await sha256(this.clientPublicKeyBytes, m, padBytes(bigIntToBytes(this.sharedKey), 32));
    }
    async getEncryptionKey() {
      if (this.sharedKey === void 0) return void 0;
      if (this._encryptionKey) return this._encryptionKey;
      const key = padBytes(bigIntToBytes(this.sharedKey), 32).slice(0, 16);
      this._encryptionKey = crypto.subtle.importKey("raw", key, "AES-GCM", false, ["encrypt", "decrypt"]);
      try {
        return await this._encryptionKey;
      } catch (error) {
        this._encryptionKey = void 0;
        throw error;
      }
    }
    // outbound framing is ciphertext+tag || iv (IV appended last). Apple's
    // SecretSession.encrypt does concat(ciphertext, iv), helper expects that
    async encrypt(obj) {
      const key = await this.getEncryptionKey();
      if (!key) throw new Error("missing encryption key");
      const iv = randomBytes(16);
      const plaintext = utf8ToBytes(JSON.stringify(obj));
      const ct = new Uint8Array(await crypto.subtle.encrypt({ name: "AES-GCM", iv }, key, plaintext));
      return concatBytes(ct, iv);
    }
    // inbound framing is iv || ciphertext+tag. Apple's SecretSession.decrypt reads
    // the IV as the first 16 bytes (bitSlice(e,0,keyLen)), Firefox ref does the same.
    // asymmetric with encrypt() on purpose, we never decrypt our own output
    async decrypt(bytes) {
      if (!(bytes instanceof Uint8Array) || bytes.length < 32) throw new Error("truncated encrypted message");
      const key = await this.getEncryptionKey();
      if (!key) throw new Error("missing encryption key");
      const iv = bytes.slice(0, 16);
      const ct = bytes.slice(16);
      const pt = new Uint8Array(await crypto.subtle.decrypt({ name: "AES-GCM", iv }, key, ct));
      return pt;
    }
  };

  // src/protocol.js
  var NATIVE_HOST = "com.apple.passwordmanager";
  var BROWSER_NAME = "Chrome";
  var VERSION = "1.0";
  var PASSWORD_READ_TIMEOUT_MS = 2 * 6e4;
  var CHALLENGE_TTL_MS = 3 * 6e4;
  function challengeError(message) {
    const e = new Error(message);
    e.code = "challenge_reissued";
    return e;
  }
  var Command = {
    END: 0,
    HANDSHAKE: 2,
    GET_LOGIN_NAMES_FOR_URL: 4,
    GET_PASSWORD_FOR_LOGIN_NAME: 5,
    SET_PASSWORD_FOR_LOGIN_NAME_URL: 6,
    // save or update a login
    TAB_EVENT: 8,
    PASSWORDS_DISABLED: 9,
    RELOGIN_NEEDED: 10,
    GET_CAPABILITIES: 14
  };
  var Action = { UPDATE: 1, SEARCH: 2, ADD_NEW: 3, MAYBE_ADD: 4, GHOST_SEARCH: 5 };
  var State = {
    Disconnected: "disconnected",
    NeedsPin: "needs_pin",
    // challenge issued, waiting for the user's PIN
    Unlocked: "unlocked",
    // session key established
    NoHelper: "no_helper"
    // native host missing
  };
  function jsonToBase64(obj) {
    return bytesToBase64(new TextEncoder().encode(JSON.stringify(obj)));
  }
  var ApplePasswords = class {
    constructor() {
      this.port = void 0;
      this.session = void 0;
      this.capabilities = void 0;
      this.state = State.Disconnected;
      this._waiters = /* @__PURE__ */ new Map();
      this._onState = () => {
      };
      this._challengeAt = 0;
      this._challengeGen = 0;
      this._challengePending = void 0;
      this._connectPending = void 0;
      this._lock = Promise.resolve();
      this.epoch = 0;
      this._jobs = /* @__PURE__ */ new Set();
    }
    _rejectWaiters(error = new Error("connection closed")) {
      for (const [cmd, waiter] of this._waiters) {
        if (cmd === Command.SET_PASSWORD_FOR_LOGIN_NAME_URL) error.mayHaveBeenSent = true;
        if (waiter.timer) clearTimeout(waiter.timer);
        waiter.reject(error);
      }
      this._waiters.clear();
    }
    _dropConnection(port, state = State.Disconnected, error = new Error("connection closed")) {
      if (port && port !== this.port) return;
      this.port = void 0;
      this.session = void 0;
      this.capabilities = void 0;
      this._challengeAt = 0;
      this._challengePending = void 0;
      this._challengeGen++;
      this.epoch++;
      this._rejectWaiters(error);
      for (const job of this._jobs) job.cancel(error);
      this._setState(state);
    }
    _withLock(fn, { signal, timeoutMs = 8e3 } = {}) {
      const epoch = this.epoch;
      let rejectJob;
      let timer;
      const job = { started: false, cancelled: false, cancel(error) {
        if (job.cancelled) return;
        job.cancelled = true;
        rejectJob(error);
      } };
      const cancelled = new Promise((_, reject) => {
        rejectJob = reject;
      });
      const abort = () => {
        const error = signal?.reason instanceof Error ? signal.reason : new Error("operation cancelled");
        if (job.started) this.disconnect(error);
        else job.cancel(error);
      };
      this._jobs.add(job);
      signal?.addEventListener("abort", abort, { once: true });
      if (signal?.aborted) abort();
      if (timeoutMs != null) timer = setTimeout(() => {
        const error = new Error("operation timed out");
        if (job.started) this.disconnect(error);
        else job.cancel(error);
      }, timeoutMs);
      const run = this._lock.then(() => {
        if (job.cancelled || epoch !== this.epoch) throw new Error("session changed");
        job.started = true;
        return Promise.race([Promise.resolve().then(fn), cancelled]);
      });
      const result = Promise.race([run, cancelled]);
      this._lock = run.catch(() => {
      });
      return result.finally(() => {
        clearTimeout(timer);
        signal?.removeEventListener("abort", abort);
        this._jobs.delete(job);
      });
    }
    _assertSession(session, epoch, unlocked = true) {
      if (this.session !== session || this.epoch !== epoch || !this.port || unlocked && !this.ready) {
        throw new Error("session changed");
      }
    }
    _queryError(status) {
      const error = queryStatusError(status);
      if (status === QueryStatus.InvalidSession) this.disconnect(error);
      return error;
    }
    onStateChange(fn) {
      this._onState = fn;
    }
    _setState(s) {
      if (this.state === s) return;
      this.state = s;
      try {
        this._onState(s);
      } catch (_) {
      }
    }
    get ready() {
      return this.port !== void 0 && this.session !== void 0 && this.session.sharedKey !== void 0 && this.state === State.Unlocked;
    }
    _send(cmd, body = {}, timeoutMs = 5e3) {
      if (!this.port) throw new Error("connection closed");
      if (this._waiters.has(cmd)) return Promise.reject(new Error("another request is already in flight"));
      return new Promise((resolve, reject) => {
        const entry = { resolve, reject, timer: null };
        entry.timer = timeoutMs == null ? null : setTimeout(() => {
          if (this._waiters.get(cmd) !== entry) return;
          const error = new Error("timeout waiting for response");
          if (cmd === Command.SET_PASSWORD_FOR_LOGIN_NAME_URL) error.mayHaveBeenSent = true;
          this.disconnect(error);
        }, timeoutMs);
        this._waiters.set(cmd, entry);
        try {
          this.port.postMessage({ cmd, ...body });
        } catch (e) {
          this.disconnect(e);
        }
      });
    }
    _dispatch(message, port = this.port) {
      if (!port || port !== this.port || !message || !Number.isInteger(message.cmd)) return;
      if (message.cmd === Command.PASSWORDS_DISABLED || message.cmd === Command.RELOGIN_NEEDED) {
        this.disconnect(new Error("invalid session"));
        return;
      }
      const w = this._waiters.get(message.cmd);
      if (w) {
        this._waiters.delete(message.cmd);
        if (w.timer) clearTimeout(w.timer);
        w.resolve(message);
      }
    }
    // does NOT reset an existing unlocked session (core fix vs Apple's extension,
    // which re-pairs on every connect)
    connect() {
      if (this.ready || this.port && this.session) return Promise.resolve();
      if (this._connectPending) return this._connectPending;
      if (this.port) {
        const stale = this.port;
        try {
          stale.disconnect();
        } catch (_) {
        }
        this._dropConnection(stale);
      }
      const pending = new Promise((resolve, reject) => {
        let port;
        try {
          port = chrome.runtime.connectNative(NATIVE_HOST);
        } catch (e) {
          this._setState(State.NoHelper);
          return reject(e);
        }
        this.port = port;
        port.onMessage.addListener((msg) => this._dispatch(msg, port));
        port.onDisconnect.addListener(() => {
          const message = chrome.runtime.lastError?.message || "native helper disconnected";
          const state = /not found|forbidden|host/i.test(message) ? State.NoHelper : State.Disconnected;
          this._dropConnection(port, state, new Error(message));
        });
        this._send(Command.GET_CAPABILITIES).then((reply) => {
          if (this.port !== port) throw new Error("connection changed");
          this.capabilities = reply.capabilities ?? {};
          if (this.capabilities.secretSessionVersion !== void 0 && this.capabilities.secretSessionVersion !== SecretSessionVersion.SRPWithRFCVerification) {
            throw new Error("unsupported capabilities (expected SRP RFC verification)");
          }
          this.session = new SRPSession(this.capabilities.shouldUseBase64);
          this._setState(State.NeedsPin);
          resolve();
        }).catch((error) => {
          if (this.port === port) {
            try {
              port.disconnect();
            } catch (_) {
            }
            this._dropConnection(port, State.Disconnected, error);
          }
          reject(error);
        });
      });
      const wrapped = pending.finally(() => {
        if (this._connectPending === wrapped) this._connectPending = void 0;
      });
      this._connectPending = wrapped;
      return wrapped;
    }
    // is there a challenge the user can still answer? the code on the Mac only belongs to
    // the newest challenge, so anything else must be re-issued before we verify
    get hasChallenge() {
      return this.state === State.NeedsPin && this.session !== void 0 && this.session.serverPublicKey !== void 0 && this.session.salt !== void 0 && Date.now() - this._challengeAt < CHALLENGE_TTL_MS;
    }
    // ask the helper for a challenge. macOS shows the 6-digit PIN access prompt.
    // ifNeeded keeps a live prompt alive instead of putting a second code on screen and
    // silently invalidating the one the user is reading
    requestChallenge({ ifNeeded = false, signal } = {}) {
      if (!this.session) return Promise.reject(new Error("not connected"));
      if (ifNeeded && (this.hasChallenge || this.state === State.Unlocked)) return Promise.resolve(false);
      if (this._challengePending) return this._challengePending;
      const p = this._withLock(() => this._issueChallenge(), { signal });
      this._challengePending = p;
      const clear = () => {
        if (this._challengePending === p) this._challengePending = void 0;
      };
      p.then(clear, clear);
      return p;
    }
    async _issueChallenge() {
      const session = new SRPSession(this.capabilities?.shouldUseBase64);
      const epoch = this.epoch;
      this.session = session;
      this._setState(State.NeedsPin);
      this._challengeAt = 0;
      this._challengeGen++;
      const reply = await this._send(Command.HANDSHAKE, {
        msg: {
          QID: "m0",
          PAKE: jsonToBase64({
            TID: session.username,
            MSG: MSGType.ClientKeyExchange,
            A: session.serialize(session.clientPublicKeyBytes),
            VER: VERSION,
            PROTO: [SecretSessionVersion.SRPWithRFCVerification]
          }),
          HSTBRSR: BROWSER_NAME
        }
      });
      this._assertSession(session, epoch, false);
      const pake = JSON.parse(bytesToUtf8(base64ToBytes(reply.payload.PAKE)));
      if (pake.TID !== session.username) throw new Error("challenge for another session");
      if (pake.ErrCode !== void 0) throw new Error(`server hello error ${pake.ErrCode}`);
      if (pake.MSG.toString() !== MSGType.ServerKeyExchange.toString()) throw new Error("unexpected server message");
      if (pake.PROTO !== SecretSessionVersion.SRPWithRFCVerification) throw new Error("unsupported protocol");
      const B = bytesToBigInt(session.deserialize(pake.B));
      const s = session.deserialize(pake.s);
      session.setServerPublicKey(B, s);
      this._challengeAt = Date.now();
      this._setState(State.NeedsPin);
      return true;
    }
    // a PIN is only valid for the challenge it was displayed for. verifying it against any
    // other challenge always fails, so never quietly swap the challenge underneath the user -
    // issue a fresh one and tell the caller to ask for the NEW code
    async verifyPin(pin, { signal } = {}) {
      if (!this.session) throw new Error("not connected");
      if (!this.hasChallenge) {
        await this.requestChallenge({ signal });
        throw challengeError("Enter the new code your Mac is showing now");
      }
      const gen = this._challengeGen;
      return this._withLock(async () => {
        if (gen !== this._challengeGen) throw challengeError("Enter the new code your Mac is showing now");
        const session = this.session;
        const epoch = this.epoch;
        try {
          await session.setSharedKey(pin);
          this._assertSession(session, epoch, false);
          const m = await session.computeM();
          this._assertSession(session, epoch, false);
          const reply = await this._send(Command.HANDSHAKE, {
            msg: {
              QID: "m2",
              PAKE: jsonToBase64({
                TID: session.username,
                MSG: MSGType.ClientVerification,
                M: session.serialize(m, false)
              })
            }
          });
          this._assertSession(session, epoch, false);
          const pake = JSON.parse(bytesToUtf8(base64ToBytes(reply.payload.PAKE)));
          if (pake.TID !== session.username) throw new Error("verification for another session");
          if (pake.MSG.toString() !== MSGType.ServerVerification.toString()) throw new Error("unexpected server message");
          if (pake.ErrCode === 1) throw new Error("Incorrect code");
          if (pake.ErrCode !== 0 && pake.ErrCode !== void 0) throw new Error(`verification error ${pake.ErrCode}`);
          const hamk = await session.computeHMAC(m);
          this._assertSession(session, epoch, false);
          if (!constantTimeEqual(session.deserialize(pake.HAMK), hamk))
            throw new Error("server HAMK mismatch");
          this._setState(State.Unlocked);
        } catch (e) {
          if (this.session === session) {
            session.sharedKey = void 0;
            session._encryptionKey = void 0;
            session.serverPublicKey = void 0;
            session.salt = void 0;
          }
          this._challengeAt = 0;
          throw e;
        }
      }, { signal });
    }
    async _encryptedQuery(cmd, tabId, hostname, payloadBody, timeoutMs) {
      const session = this.session;
      const epoch = this.epoch;
      this._assertSession(session, epoch);
      const sdata = session.serialize(await session.encrypt(payloadBody));
      this._assertSession(session, epoch);
      const reply = await this._send(
        cmd,
        {
          tabId,
          frameId: 0,
          url: hostname,
          payload: { QID: cmd === Command.GET_LOGIN_NAMES_FOR_URL ? "CmdGetLoginNames4URL" : "CmdGetPassword4LoginName", SMSG: JSON.stringify({ TID: session.username, SDATA: sdata }) }
        },
        timeoutMs
      );
      try {
        this._assertSession(session, epoch);
        let smsg = reply.payload.SMSG;
        if (typeof smsg === "string") smsg = JSON.parse(smsg);
        if (smsg.TID !== session.username) throw new Error("response for another session");
        const data = await session.decrypt(session.deserialize(smsg.SDATA));
        this._assertSession(session, epoch);
        return JSON.parse(bytesToUtf8(data));
      } catch (error) {
        if (this.session === session && this.epoch === epoch) this.disconnect(error);
        throw error;
      }
    }
    async getLoginNamesForURL(tabId, url, { signal } = {}) {
      if (!this.ready) throw new Error("not unlocked");
      const { hostname } = new URL(url);
      return this._withLock(async () => {
        const res = await this._encryptedQuery(
          Command.GET_LOGIN_NAMES_FOR_URL,
          tabId,
          hostname,
          { ACT: Action.GHOST_SEARCH, URL: hostname },
          5e3
        );
        if (res.STATUS === QueryStatus.Success)
          return (res.Entries ?? []).map((e) => ({ username: e.USR, sites: e.sites }));
        if (res.STATUS === QueryStatus.NoResults) return [];
        throw this._queryError(res.STATUS);
      }, { signal, timeoutMs: 8e3 });
    }
    async getPasswordForLoginName(tabId, url, loginName, { signal } = {}) {
      if (!this.ready) throw new Error("not unlocked");
      const { hostname } = new URL(url);
      return this._withLock(async () => {
        let res;
        try {
          res = await this._encryptedQuery(
            Command.GET_PASSWORD_FOR_LOGIN_NAME,
            tabId,
            // Query by trusted frame hostname, never caller-supplied loginName.sites,
            // which a page could use to request another origin's password.
            hostname,
            { ACT: Action.SEARCH, URL: hostname, USR: loginName.username },
            PASSWORD_READ_TIMEOUT_MS
          );
        } catch (error) {
          if (/timeout waiting for response/i.test(String(error?.message ?? error))) {
            this.disconnect();
            throw new Error("Password request timed out; try again");
          }
          throw error;
        }
        if (res.STATUS === QueryStatus.Success) {
          const e = (res.Entries ?? []).find((entry) => entry.USR === loginName.username);
          if (!e) return void 0;
          return { username: e.USR, password: e.PWD, sites: e.sites };
        }
        if (res.STATUS === QueryStatus.NoResults) return void 0;
        throw this._queryError(res.STATUS);
      }, { signal, timeoutMs: PASSWORD_READ_TIMEOUT_MS + 8e3 });
    }
    // Apple's client posts cmd 6 without awaiting a reply. Waiting here blocks all
    // queries and eventually closes the native confirmation UI. A successful post
    // is only handoff; it does not establish that the user saved the credential.
    async saveLogin(tabId, url, username, password, { signal, expiresAt = Infinity, frameId = 0 } = {}) {
      if (!this.ready) throw new Error("not unlocked");
      if (!password) throw new Error("no password to save");
      const { host } = new URL(url);
      return this._withLock(async () => {
        const session = this.session;
        const epoch = this.epoch;
        this._assertSession(session, epoch);
        if (Date.now() >= expiresAt) throw new Error("save expired");
        const sdata = session.serialize(
          await session.encrypt({
            ACT: Action.MAYBE_ADD,
            URL: "",
            USR: "",
            PWD: "",
            NURL: host,
            NUSR: username ?? "",
            NPWD: password
          })
        );
        this._assertSession(session, epoch);
        if (Date.now() >= expiresAt) throw new Error("save expired");
        const body = {
          tabId,
          frameId,
          payload: JSON.stringify({
            QID: "CmdSetPassword4LoginName_URL",
            SMSG: JSON.stringify({ TID: session.username, SDATA: sdata })
          })
        };
        try {
          this.port.postMessage({ cmd: Command.SET_PASSWORD_FOR_LOGIN_NAME_URL, ...body });
        } catch (error) {
          error.mayHaveBeenSent = true;
          this.disconnect(error);
          throw error;
        }
        return true;
      }, { signal });
    }
    disconnect(error = new Error("connection closed")) {
      const port = this.port;
      if (!port) {
        this._dropConnection(void 0, State.Disconnected, error);
        return;
      }
      try {
        port.postMessage({ cmd: Command.END });
      } catch (_) {
      }
      this._dropConnection(port, State.Disconnected, error);
      try {
        port.disconnect();
      } catch (_) {
      }
    }
  };

  // src/shared.js
  function normalizePin(value) {
    return String(value ?? "").normalize("NFKC").replace(/\D/g, "").slice(0, 6);
  }
  function pageContext(value) {
    try {
      const url = new URL(value);
      return {
        url,
        host: url.hostname.toLowerCase(),
        origin: url.origin,
        secure: url.protocol === "https:"
      };
    } catch {
      return null;
    }
  }
  function isLocalDevHost(host) {
    return host === "localhost" || host === "127.0.0.1" || host === "[::1]" || host?.endsWith(".localhost");
  }
  function errorKey(error) {
    if (error?.errorKey) return error.errorKey;
    const message = String(error?.message ?? error ?? "");
    if (/incorrect code/i.test(message)) return "errorIncorrectCode";
    if (/challenge_reissued|new code|expired code/i.test(error?.code || message)) return "errorNewCode";
    if (/timeout|timed out/i.test(message)) return "errorTimeout";
    if (/not unlocked|invalid session|session changed/i.test(message)) return "errorLocked";
    if (/not found|forbidden.*host|native.*host/i.test(message)) return "errorNoHelper";
    if (/connection|disconnected|not connected/i.test(message)) return "errorConnection";
    if (/page|frame|document|target|stale|superseded/i.test(message)) return "errorPageChanged";
    if (/cancel|abort/i.test(message)) return "errorCancelled";
    if (/HTTPS|URL|unavailable/i.test(message)) return "pageUnavailable";
    if (/protocol|capabilities|HAMK|authentication|encoding|hex|base64|format|payload|message|session/i.test(message)) return "errorProtocol";
    return "errorOperation";
  }
  async function activeTab() {
    const [tab] = await chrome.tabs.query({ active: true, currentWindow: true });
    return tab;
  }

  // src/session-cache.js
  var SessionCache = class {
    constructor(ttl, usable = () => true, limit = 100) {
      this.ttl = ttl;
      this.usable = usable;
      this.limit = limit;
      this.entries = /* @__PURE__ */ new Map();
      this.pending = /* @__PURE__ */ new Map();
      this.generation = 0;
    }
    get(key) {
      const entry = this.entries.get(key);
      if (!this.usable() || !entry || Date.now() >= entry.expiresAt) {
        this.remove(key);
        return void 0;
      }
      return entry.value;
    }
    set(key, value, generation = this.generation) {
      if (!this.usable() || generation !== this.generation) throw new Error("session changed");
      this.remove(key);
      const entry = { value, expiresAt: Date.now() + this.ttl };
      entry.timer = setTimeout(() => {
        if (this.entries.get(key) === entry) this.remove(key);
      }, this.ttl);
      this.entries.set(key, entry);
      while (this.entries.size > this.limit) this.remove(this.entries.keys().next().value);
      return value;
    }
    remove(key) {
      const entry = this.entries.get(key);
      if (entry) clearTimeout(entry.timer);
      this.entries.delete(key);
    }
    clear() {
      this.generation++;
      for (const key of this.entries.keys()) this.remove(key);
      this.pending.clear();
    }
    async query(key, loader) {
      const hit = this.get(key);
      if (hit !== void 0) return hit;
      if (this.pending.has(key)) return this.pending.get(key);
      const generation = this.generation;
      const result = Promise.resolve().then(loader).then((value) => this.set(key, value, generation));
      this.pending.set(key, result);
      try {
        return await result;
      } finally {
        if (this.pending.get(key) === result) this.pending.delete(key);
      }
    }
  };
  var credentialKey = (origin, username) => JSON.stringify([origin, username ?? ""]);

  // src/unlock-flow.js
  async function beginInlineUnlock(client2, ensureConnected2, openPopup) {
    await ensureConnected2();
    if (!client2.ready) await client2.requestChallenge();
    const challengeReady = client2.hasChallenge;
    if (!client2.ready && !challengeReady) throw new Error("No verification challenge received");
    let popupOpened = false;
    try {
      await openPopup();
      popupOpened = true;
    } catch {
    }
    return { ok: true, popupOpened, state: client2.state, challengeReady };
  }

  // src/toolbar-status.js
  var icons = { 16: "icons/icon16.png", 48: "icons/icon48.png", 128: "icons/icon128.png" };
  var titleKeys = {
    connecting: "toolbarConnecting",
    disconnected: "toolbarDisconnected",
    no_helper: "toolbarNoHelper",
    needs_pin: "toolbarLocked",
    unlocked: "toolbarUnlocked"
  };
  async function disconnectedIcon(runtime) {
    const entries = await Promise.all(Object.entries(icons).map(async ([size, path]) => {
      const response = await fetch(runtime.getURL(path));
      if (!response.ok) throw new Error("Toolbar icon unavailable");
      const bitmap = await createImageBitmap(await response.blob());
      try {
        const canvas = new OffscreenCanvas(Number(size), Number(size));
        const context = canvas.getContext("2d");
        context.fillStyle = "#ef5350";
        context.fillRect(0, 0, canvas.width, canvas.height);
        context.globalCompositeOperation = "multiply";
        context.drawImage(bitmap, 0, 0, canvas.width, canvas.height);
        return [size, context.getImageData(0, 0, canvas.width, canvas.height)];
      } finally {
        bitmap.close();
      }
    }));
    return Object.fromEntries(entries);
  }
  function createToolbarStatus(api, loadFailureIcon = () => disconnectedIcon(api.runtime)) {
    const normalIcons = Object.fromEntries(Object.entries(icons).map(([size, path]) => [size, api.runtime.getURL(path)]));
    let desired = null;
    let applied = null;
    let running = null;
    let failureIcon = null;
    async function render() {
      while (desired !== applied) {
        const status = desired;
        const notReady = status !== "unlocked";
        let details = { path: normalIcons };
        if (notReady) {
          failureIcon ||= await loadFailureIcon();
          if (status !== desired) continue;
          details = { imageData: failureIcon };
        }
        const title = api.i18n.getMessage(titleKeys[status]);
        const results = await Promise.allSettled([
          api.action.setIcon(details),
          api.action.setTitle({ title })
        ]);
        for (const result of results) if (result.status === "rejected") throw result.reason;
        applied = status;
      }
    }
    return {
      update(state, connecting2 = false) {
        if (!api.action?.setIcon || !api.action?.setTitle) return Promise.resolve();
        desired = connecting2 ? "connecting" : state;
        if (!running) {
          let completed = false;
          running = render().then(() => {
            completed = true;
          }).catch(() => {
            console.warn("FAPassword toolbar status could not be updated");
          }).finally(() => {
            running = null;
            if (completed && desired !== applied) this.update(desired);
          });
        }
        return running;
      }
    };
  }

  // src/background.js
  var client = new ApplePasswords();
  var toolbar = createToolbarStatus(chrome);
  var connectionAttempt = null;
  var connecting = false;
  var passwordCache = new SessionCache(12e4, () => client.ready);
  var loginCache = new SessionCache(15e3, () => client.ready);
  var frames = /* @__PURE__ */ new Map();
  var uiTargets = /* @__PURE__ */ new Map();
  var operations = /* @__PURE__ */ new Map();
  var uiPorts = /* @__PURE__ */ new Set();
  var lastFillByTab = /* @__PURE__ */ new Map();
  var lastFocusByTab = /* @__PURE__ */ new Map();
  var mruByHost = /* @__PURE__ */ new Map();
  var recentFills = /* @__PURE__ */ new Map();
  var generated = /* @__PURE__ */ new Map();
  var pendingSaves = /* @__PURE__ */ new Map();
  var SAVE_TTL = 18e4;
  var flushing = false;
  var lastConnectionError = null;
  var revision = 0;
  var ownUi = (sender) => sender.id === chrome.runtime.id && sender.url === chrome.runtime.getURL("src/popup.html");
  var frameKey = (tabId, frameId) => `${tabId}:${frameId}`;
  var operationKey = (documentId, requestId) => `${documentId}:${requestId}`;
  var usablePage = (url) => {
    const context = pageContext(url);
    return context && ["http:", "https:"].includes(context.url.protocol) && (context.secure || isLocalDevHost(context.host)) ? context : null;
  };
  var failure = (error) => ({ ok: false, errorKey: errorKey(error), error: String(error?.message ?? error), state: client.state });
  var digest = async (value) => Array.from(
    new Uint8Array(await crypto.subtle.digest("SHA-256", new TextEncoder().encode(value))),
    (byte) => byte.toString(16).padStart(2, "0")
  ).join("");
  function publicSaves() {
    return [...pendingSaves.values()].map(({ id, host, username, status, expiresAt, messageKey }) => ({ id, host, username, status, expiresAt, messageKey }));
  }
  function publish() {
    const message = { type: "state", state: client.state, revision: ++revision, saves: publicSaves() };
    for (const port of uiPorts) {
      try {
        port.postMessage(message);
      } catch {
      }
    }
  }
  function clearCaches() {
    passwordCache.clear();
    loginCache.clear();
  }
  client.onStateChange((state) => {
    toolbar.update(state, connecting);
    if (state !== State.Unlocked) {
      clearCaches();
      recentFills.clear();
      for (const operation of operations.values()) operation.abort(new Error("session changed"));
    }
    publish();
    if (state === State.Unlocked) queueMicrotask(flushPendingSaves);
  });
  chrome.runtime.onConnect.addListener((port) => {
    if (port.name !== "fapassword-popup" || !ownUi(port.sender || {})) return;
    uiPorts.add(port);
    port.onDisconnect.addListener(() => uiPorts.delete(port));
    port.postMessage({ type: "state", state: client.state, revision, saves: publicSaves() });
  });
  function recordMru(origin, username) {
    const values = (mruByHost.get(origin) || []).filter((value) => value !== username);
    mruByHost.delete(origin);
    mruByHost.set(origin, [username, ...values].slice(0, 10));
    while (mruByHost.size > 100) mruByHost.delete(mruByHost.keys().next().value);
  }
  function orderLogins(origin, logins) {
    const seen = /* @__PURE__ */ new Set();
    const order = mruByHost.get(origin) || [];
    const rank = (username) => {
      const index = order.indexOf(username);
      return index < 0 ? Infinity : index;
    };
    return logins.filter((login) => {
      if (typeof login.username !== "string" || seen.has(login.username)) return false;
      seen.add(login.username);
      return true;
    }).sort((a, b) => rank(a.username) - rank(b.username));
  }
  function getLoginNames(tabId, url) {
    const context = usablePage(url);
    if (!context) throw new Error("unavailable URL");
    return loginCache.query(context.origin, () => client.getLoginNamesForURL(tabId, url));
  }
  function ensureConnected() {
    if (connectionAttempt) return connectionAttempt;
    if (client.port && client.session) return Promise.resolve(true);
    connecting = true;
    toolbar.update(client.state, connecting);
    connectionAttempt = Promise.resolve().then(() => client.connect()).then(() => {
      lastConnectionError = null;
      return true;
    }, (error) => {
      lastConnectionError = errorKey(error);
      return false;
    }).finally(() => {
      connecting = false;
      connectionAttempt = null;
      toolbar.update(client.state);
    });
    return connectionAttempt;
  }
  function cancelDocument(documentId) {
    for (const [key, operation] of operations) if (key.startsWith(documentId + ":")) operation.abort(new Error("document changed"));
    recentFills.delete(documentId);
    generated.delete(documentId);
  }
  chrome.tabs.onRemoved.addListener((tabId) => {
    lastFillByTab.delete(tabId);
    lastFocusByTab.delete(tabId);
    for (const [key, frame] of frames) if (frame.tabId === tabId) {
      cancelDocument(frame.documentId);
      frames.delete(key);
    }
    for (const [key, target] of uiTargets) if (target.tabId === tabId) uiTargets.delete(key);
  });
  function senderTarget(sender, documentKey) {
    const context = usablePage(sender.url);
    if (!context || sender.tab?.id == null || !Number.isInteger(sender.frameId) || !sender.documentId || typeof documentKey !== "string") {
      throw new Error("unavailable frame URL");
    }
    return {
      tabId: sender.tab.id,
      frameId: sender.frameId,
      documentId: sender.documentId,
      documentKey,
      origin: context.origin,
      frameUrl: sender.url,
      topOrigin: pageContext(sender.tab.url)?.origin
    };
  }
  function rememberFrame(target) {
    const key = frameKey(target.tabId, target.frameId);
    const previous = frames.get(key);
    if (previous && previous.documentId !== target.documentId) cancelDocument(previous.documentId);
    frames.set(key, target);
  }
  async function sendToDocument(target, message) {
    try {
      return await chrome.tabs.sendMessage(
        target.tabId,
        { ...message, documentKey: target.documentKey },
        { documentId: target.documentId, frameId: target.frameId }
      );
    } catch {
      throw new Error("The page document changed; try again");
    }
  }
  async function captureTopTarget(tab) {
    if (!tab?.id || !usablePage(tab.url)) throw new Error("unavailable page URL");
    let response;
    try {
      response = await chrome.tabs.sendMessage(tab.id, { type: "describeFrame" }, { frameId: 0 });
    } catch {
      throw new Error("unavailable page URL");
    }
    const frame = frames.get(frameKey(tab.id, 0));
    if (!response?.ok || !frame || response.documentKey !== frame.documentKey || frame.origin !== pageContext(tab.url).origin) {
      throw new Error("page document changed");
    }
    return { ...frame };
  }
  function ticketFor(target) {
    for (const [id2, value] of uiTargets) if (Date.now() - value.at > 18e4) uiTargets.delete(id2);
    const id = crypto.randomUUID();
    uiTargets.set(id, { ...target, at: Date.now() });
    while (uiTargets.size > 50) uiTargets.delete(uiTargets.keys().next().value);
    return id;
  }
  async function validateUiTarget(id) {
    const target = uiTargets.get(id);
    const tab = await activeTab();
    if (!target || Date.now() - target.at > 18e4 || target.tabId !== tab?.id || target.topOrigin !== pageContext(tab?.url)?.origin) throw new Error("stale page target");
    const response = await sendToDocument(target, { type: "describeFrame" });
    if (!response?.ok) throw new Error("page document changed");
    return target;
  }
  async function performFill(target, username, requestId) {
    if (typeof username !== "string" || typeof requestId !== "string") throw new Error("invalid fill message");
    const key = operationKey(target.documentId, requestId);
    if (operations.has(key)) throw new Error("duplicate fill request");
    const controller = new AbortController();
    operations.set(key, controller);
    const generation = passwordCache.generation;
    const epoch = client.epoch;
    try {
      if (!client.ready) throw new Error("not unlocked");
      const cacheKey = credentialKey(target.origin, username);
      let credential = passwordCache.get(cacheKey);
      if (!credential) {
        credential = await client.getPasswordForLoginName(target.tabId, target.frameUrl, { username }, { signal: controller.signal });
        if (!credential) return { ok: false, errorKey: "fillFailed", filled: false };
        if (credential.username !== username || !client.ready || epoch !== client.epoch || controller.signal.aborted) throw new Error("session changed");
        passwordCache.set(cacheKey, credential, generation);
      }
      if (!client.ready || generation !== passwordCache.generation || controller.signal.aborted) throw new Error("session changed");
      const recent = { username, digest: digest(credential.password), at: Date.now() };
      recentFills.set(target.documentId, recent);
      const response = await sendToDocument(target, {
        type: "fill",
        requestId,
        expectedOrigin: target.origin,
        username: credential.username,
        password: credential.password
      });
      if (!response?.filled && recentFills.get(target.documentId) === recent) recentFills.delete(target.documentId);
      if (response?.filled) {
        recordMru(target.origin, username);
        lastFillByTab.set(target.tabId, { ...target, username });
      }
      return {
        ok: !!response?.filled,
        filled: !!response?.filled,
        fields: response?.fields,
        stage: response?.stage,
        errorKey: response?.errorKey || (response?.filled ? void 0 : "errorPartialFill")
      };
    } finally {
      operations.delete(key);
    }
  }
  function setSaveStatus(entry, status, messageKey) {
    entry.status = status;
    entry.messageKey = messageKey;
    publish();
    sendToDocument(entry.target, { type: "saveStatus", messageKey }).catch(() => {
    });
  }
  function discardSecret(entry, status, messageKey) {
    clearTimeout(entry.timer);
    clearTimeout(entry.retryTimer);
    entry.password = "";
    entry.controller?.abort(new Error(status === "expired" ? "save expired" : "save cancelled"));
    setSaveStatus(entry, status, messageKey);
  }
  function stillPending(entry) {
    if (!entry.password || entry.status === "cancelled" || entry.status === "expired") return false;
    if (Date.now() >= entry.expiresAt) {
      discardSecret(entry, "expired", "saveExpired");
      return false;
    }
    return true;
  }
  function queueSave(message, target) {
    if (typeof message.password !== "string" || !message.password || typeof message.username !== "string") throw new Error("invalid save message");
    const entry = {
      id: crypto.randomUUID(),
      target,
      host: new URL(target.frameUrl).hostname,
      username: message.username,
      password: message.password,
      newPwCtx: !!message.newPwCtx,
      generatedId: message.generatedId,
      submissionId: message.submissionId,
      expiresAt: Date.now() + SAVE_TTL,
      status: "queued",
      attempts: 0,
      controller: null
    };
    entry.signature = digest(JSON.stringify([target.origin, entry.username, entry.password]));
    entry.timer = setTimeout(() => discardSecret(entry, "expired", "saveExpired"), SAVE_TTL);
    pendingSaves.set(entry.id, entry);
    while (pendingSaves.size > 10) {
      const oldest = pendingSaves.values().next().value;
      discardSecret(oldest, "expired", "saveExpired");
      pendingSaves.delete(oldest.id);
    }
    setSaveStatus(entry, "queued", client.ready ? "saveQueued" : "saveWaitingUnlock");
    queueMicrotask(flushPendingSaves);
    return { ok: true, saveId: entry.id, status: entry.status };
  }
  async function flushPendingSaves() {
    if (flushing) return;
    flushing = true;
    try {
      const queued = [...pendingSaves.values()].filter((entry) => entry.status === "queued" && stillPending(entry));
      if (!queued.length) return;
      if (!client.ready) await ensureConnected();
      if (!client.ready) {
        for (const entry of queued) if (stillPending(entry)) setSaveStatus(entry, "queued", "saveWaitingUnlock");
        return;
      }
      for (const entry of queued) {
        if (!stillPending(entry) || !client.ready) continue;
        try {
          const signature = await entry.signature;
          if (!stillPending(entry)) continue;
          const duplicate = [...pendingSaves.values()].some((other) => other !== entry && other.completedAt && Date.now() - other.completedAt < 15e3 && other.resolvedSignature === signature);
          const record = recentFills.get(entry.target.documentId);
          const passwordDigest = await digest(entry.password);
          if (!stillPending(entry)) continue;
          const unchanged = record && record.username === entry.username && Date.now() - record.at < 3e5 && await record.digest === passwordDigest;
          if (!stillPending(entry)) continue;
          if (duplicate || unchanged) {
            discardSecret(entry, "skipped", "saveUnchanged");
            continue;
          }
          const generatedRecord = generated.get(entry.target.documentId);
          const wasGenerated = !!generatedRecord && generatedRecord.id === entry.generatedId && Date.now() - generatedRecord.at < 6e5 && await generatedRecord.digest === passwordDigest;
          if (!stillPending(entry)) continue;
          if (!entry.username && !entry.newPwCtx && !wasGenerated) {
            discardSecret(entry, "skipped", "saveNoAccount");
            continue;
          }
          entry.controller = new AbortController();
          entry.attempts++;
          setSaveStatus(entry, "saving", "saveSending");
          await client.saveLogin(
            entry.target.tabId,
            entry.target.frameUrl,
            entry.username,
            entry.password,
            { signal: entry.controller.signal, expiresAt: entry.expiresAt, frameId: entry.target.frameId }
          );
          if (!stillPending(entry)) continue;
          clearCaches();
          entry.controller = null;
          entry.resolvedSignature = signature;
          entry.completedAt = Date.now();
          discardSecret(entry, "submitted", "saveSubmitted");
        } catch (error) {
          if (!stillPending(entry)) continue;
          entry.controller = null;
          console.warn("[FAPassword] deferred save failed", error);
          if (error.mayHaveBeenSent) setSaveStatus(entry, "uncertain", "saveUncertain");
          else {
            setSaveStatus(entry, "failed", "saveFailed");
            if (entry.attempts < 3) entry.retryTimer = setTimeout(() => {
              if (!stillPending(entry) || entry.status !== "failed") return;
              setSaveStatus(entry, "queued", "saveQueued");
              flushPendingSaves();
            }, 2e3 * Math.max(entry.attempts, 1));
          }
        }
      }
    } finally {
      flushing = false;
      if (client.ready && [...pendingSaves.values()].some((entry) => entry.status === "queued" && stillPending(entry))) queueMicrotask(flushPendingSaves);
    }
  }
  var contentAllowed = /* @__PURE__ */ new Set(["frameReady", "recordFocus", "inlineLogins", "inlineFill", "cancelFill", "resolveSave", "rememberGenerated", "beginUnlock"]);
  chrome.runtime.onMessage.addListener((message, sender, respond) => {
    (async () => {
      try {
        const fromUi = ownUi(sender);
        const fromContent = sender.id === chrome.runtime.id && sender.tab && contentAllowed.has(message?.type);
        if (!fromUi && !fromContent) throw new Error("forbidden message");
        let target;
        if (fromContent) {
          target = senderTarget(sender, message.documentKey);
          if (message.type === "frameReady") {
            rememberFrame(target);
            return respond({ ok: true });
          }
          const current = frames.get(frameKey(target.tabId, target.frameId));
          if (current && current.documentId !== target.documentId) throw new Error("stale document");
          rememberFrame(target);
        }
        switch (message.type) {
          case "getPasswordsLauncher":
          case "openPasswords":
            return respond(await new Promise((resolve) => {
              chrome.runtime.sendNativeMessage("com.fapassword.policy", {
                action: message.type === "openPasswords" ? "openPasswords" : "passwordsCapabilities"
              }, (response) => {
                const error = chrome.runtime.lastError;
                resolve(error ? { ok: false } : response || { ok: false });
              });
            }));
          case "policy":
            if (!["get", "set", "clear"].includes(message.action)) throw new Error("invalid policy action");
            return respond(await new Promise((resolve) => {
              chrome.runtime.sendNativeMessage("com.fapassword.policy", { action: message.action }, (response) => {
                const error = chrome.runtime.lastError;
                resolve(error ? { ok: false, errorKey: "policyHelperFailed" } : response || { ok: false });
              });
            }));
          case "recordFocus":
            lastFocusByTab.set(target.tabId, target);
            return respond({ ok: true });
          case "inlineLogins":
            await ensureConnected();
            if (!client.ready) return respond(client.state === State.NeedsPin ? { ok: true, locked: true, logins: [] } : { ok: false, errorKey: lastConnectionError || "errorConnection" });
            return respond({ ok: true, locked: false, logins: orderLogins(target.origin, await getLoginNames(target.tabId, target.frameUrl)) });
          case "inlineFill":
            return respond(await performFill(target, message.loginName?.username, message.requestId));
          case "cancelFill":
            operations.get(operationKey(target.documentId, message.requestId))?.abort(new Error("fill cancelled"));
            return respond({ ok: true });
          case "rememberGenerated":
            if (typeof message.password !== "string" || typeof message.generatedId !== "string") throw new Error("invalid message");
            generated.set(target.documentId, { id: message.generatedId, digest: digest(message.password), at: Date.now() });
            return respond({ ok: true });
          case "resolveSave":
            return respond(queueSave(message, target));
          case "beginUnlock":
            return respond(await beginInlineUnlock(client, ensureConnected, () => chrome.action.openPopup()));
          case "getState":
          case "retryConnection":
            await ensureConnected();
            return respond({ ok: true, state: client.state, hasChallenge: client.hasChallenge, errorKey: lastConnectionError, saves: publicSaves() });
          case "requestChallenge": {
            await ensureConnected();
            const issued = await client.requestChallenge({ ifNeeded: !!message.ifNeeded });
            return respond({ ok: true, state: client.state, hasChallenge: client.hasChallenge, issued });
          }
          case "verifyPin": {
            const pin = normalizePin(message.pin);
            if (pin.length !== 6) return respond({ ok: false, errorKey: "errorPinLength" });
            await ensureConnected();
            try {
              await client.verifyPin(pin);
            } catch (error) {
              let newCode = error.code === "challenge_reissued";
              if (!newCode && !client.hasChallenge && client.state === State.NeedsPin) {
                try {
                  await client.requestChallenge();
                  newCode = true;
                } catch {
                }
              }
              return respond({ ...failure(error), newCode });
            }
            return respond({ ok: true, state: client.state });
          }
          case "getLogins": {
            const tab = await activeTab();
            const context = usablePage(tab?.url);
            if (!tab?.id || !context) return respond({ ok: false, errorKey: "pageUnavailable" });
            let frame = lastFocusByTab.get(tab?.id);
            if (frame && frame.topOrigin === context.origin) {
              const description = await sendToDocument(frame, { type: "describeFrame" }).catch(() => null);
              if (!description?.ok) frame = null;
            } else frame = null;
            frame ||= await captureTopTarget(tab).catch(() => null);
            const url = frame?.frameUrl || tab.url;
            const origin = frame?.origin || context.origin;
            const logins = orderLogins(origin, await getLoginNames(tab.id, url));
            const currentTab = await activeTab();
            if (currentTab?.id !== tab.id || pageContext(currentTab.url)?.origin !== context.origin) {
              return respond({ ok: false, errorKey: "pageUnavailable" });
            }
            return respond({
              ok: true,
              logins,
              targetId: frame ? ticketFor(frame) : null,
              host: new URL(url).hostname,
              refill: lastFillByTab.get(tab.id) ? { username: lastFillByTab.get(tab.id).username, host: new URL(lastFillByTab.get(tab.id).frameUrl).hostname } : null
            });
          }
          case "fillOnPage": {
            const frame = await validateUiTarget(message.targetId);
            const preparation = await sendToDocument(frame, { type: "prepareFill" });
            if (!preparation?.ok) return respond(preparation || { ok: false, errorKey: "errorPageChanged" });
            return respond(await performFill(frame, message.loginName?.username, preparation.requestId));
          }
          case "refreshLogins":
            clearCaches();
            return respond({ ok: true });
          case "refillOnPage": {
            const tab = await activeTab();
            const previous = lastFillByTab.get(tab?.id);
            if (!previous || previous.topOrigin !== pageContext(tab?.url)?.origin) throw new Error("stale page target");
            const preparation = await sendToDocument(previous, { type: "prepareFill" });
            if (!preparation?.ok) return respond(preparation || { ok: false, errorKey: "errorPageChanged" });
            clearCaches();
            return respond(await performFill(previous, previous.username, preparation.requestId));
          }
          case "retrySave": {
            const entry = pendingSaves.get(message.saveId);
            if (!entry || !stillPending(entry) || entry.status === "saving") throw new Error("save expired");
            clearTimeout(entry.retryTimer);
            setSaveStatus(entry, "queued", "saveQueued");
            queueMicrotask(flushPendingSaves);
            return respond({ ok: true, saves: publicSaves() });
          }
          case "cancelSave": {
            const entry = pendingSaves.get(message.saveId);
            if (entry) {
              discardSecret(entry, "cancelled", "saveCancelled");
              pendingSaves.delete(entry.id);
              publish();
            }
            return respond({ ok: true, saves: publicSaves() });
          }
          default:
            throw new Error("unknown message");
        }
      } catch (error) {
        respond(failure(error));
      }
    })();
    return true;
  });
  function suppressChromeAutofill() {
    const svc = chrome.privacy?.services;
    if (!svc?.passwordSavingEnabled) return;
    chrome.storage?.local?.get({ suppressSaveBubble: false, suppressAddressAutofill: false }, (o) => {
      if (chrome.runtime.lastError) return;
      try {
        if (o.suppressSaveBubble) {
          svc.passwordSavingEnabled.set({ value: false }, () => void chrome.runtime.lastError);
        } else {
          svc.passwordSavingEnabled.clear({}, () => void chrome.runtime.lastError);
        }
        if (o.suppressAddressAutofill && svc.autofillAddressEnabled) {
          svc.autofillAddressEnabled.set({ value: false }, () => void chrome.runtime.lastError);
        } else if (svc.autofillAddressEnabled) {
          svc.autofillAddressEnabled.clear({}, () => void chrome.runtime.lastError);
        }
      } catch (_) {
      }
    });
  }
  chrome.runtime.onInstalled.addListener(suppressChromeAutofill);
  chrome.runtime.onStartup.addListener(suppressChromeAutofill);
  suppressChromeAutofill();
  chrome.runtime.onStartup.addListener(ensureConnected);
  chrome.runtime.onInstalled.addListener(ensureConnected);
  ensureConnected();
})();
