# FAPassword 产品与工程规范

本文件是功能开发、UI 修改和回归检查的共同约束。它描述应有行为；实际验证环境、证据和剩余人工验收记录在 [VERIFICATION.md](VERIFICATION.md)。变更行为时同步更新规范与相关测试，不能把模拟测试通过写成真实 Apple 保险库已经验收。

## 1. 产品范围

1. 在 macOS 的 Chromium 浏览器中，通过 Apple 原生助手查询登录名、按用户选择填充密码，并交接保存／更新请求。
2. 内联选择和工具栏弹窗共用后台凭据、安全及会话逻辑；提供新密码生成，不扩展为 passkey、TOTP 或二维码扫描工具。
3. 系统认证由 Apple 助手处理；平台和版本限制须明确说明，不承诺绕过系统授权。

## 2. 视觉与信息层级

1. 延续方形、紧凑、简洁的界面：细边框、系统字体、明确选中态；不加入装饰字体或夸张动效。
2. **账号优先**：内联选择框顶部直接显示条目，不放 logo、产品标题或品牌占位行。整行账号可点击，右侧同时显示与工具栏账号列表一致的 **Fill／填充** 按钮；尺寸、字体、单层边框与浅／深色配色保持一致。按钮与整行共用经过安全校验的填充入口，一次操作不能重复填充。
3. 右下角只写 **FAPassword**，使用现有系统字体、小号字和次要文字颜色；不添加图标、口号、官方身份暗示或 Apple／Chrome 说明串。
4. 品牌是独立页脚，不是账号、按钮或链接，不参与键盘选项和 Tab 顺序；长列表滚动时保持可见。
   默认缩放下页脚保持约 17px 高（不超过 18px），不得做成账号行的高度。使用清楚可见的顶部分隔线和与账号区域不同的背景，浅／深色主题均须有明确分区。
5. 浅／深色主题普通文字对比度至少 4.5:1，缩放不能让操作不可达或产生横向溢出。用户名省略显示时，完整名称仍可被辅助技术读取。
6. **操作提示统一放在当前内容下方：解锁后在账号列表下方，验证时在验证码输入区域下方。** 验证码说明、连接状态、进度、成功、失败、空账号及“不支持当前页面”共用一个状态节点，不在上下同时显示。**没有提示时高度和间距都为零，不预留空白；有内容时按文字自然高度展开。** 统一使用无底色、无外框的 12px 说明文字，距上方内容 7px，错误仅改变文字颜色，内容仍明确说明原因。设置项的常驻说明使用同样文字样式。
7. Unlock 保持平面按钮，只保留一层均匀边框；不使用底部内阴影、额外灰线或立体效果。
8. 弹窗底部提供两个固定的密码工具操作：打开 Apple 密码入口，以及生成强密码。它们使用与设置区一致的方形按钮和系统字体，不挤占账号列表；打开操作不依赖浏览器策略助手。通过 UA Client Hints 的实际系统版本选择入口，不使用被冻结的普通 User-Agent：macOS 13／14 进入密码设置，15 及以后进入“自动填充与密码”，提示用户从那里打开独立密码 App；未知版本进入 Apple 官方操作说明。不把 Help Viewer 专用动作当成通用浏览器协议，不把已发出打开请求显示成 App 已打开。各系统实际跳转需独立验收。生成结果只保留在当前弹窗内，用户明确点击复制后才写入剪贴板，不写入后台、日志或持久化存储。生成面板中的长度和“包含特殊字符”选项必须保持单行显示。
9. Browser controls 标题不显示固定数量标记；展开控件使用直观的下拉箭头。键盘操作可以聚焦和展开，但不显示浏览器默认的蓝色焦点外框或被窗口裁切的残缺边框。
10. 弹窗宽度保持 350px。设置名称和提示优先通过精简中英文措辞适配单行，不因冗长文案自动扩宽。设置文字保持 12px，必须完整显示，不允许裁切、隐藏或用省略号替代；放大或特殊内容确实放不下时允许换行。提示保留必要操作，例如安装助手的命令。

## 3. 内联选择框

1. **任何条目数量下都不显示水平或垂直滚动条**，包括系统设为始终显示滚动条时；不能只依赖操作系统自动隐藏。
2. 少量条目自然展开；大量条目限制高度并保留内部滚动。滚轮、触控板、触摸和方向键仍可到达全部条目，不能通过裁掉账号隐藏滚动条。
3. 列表贴近所操作字段并限制在可视视口内，必要时向上展开。位置更新由动画帧合并，先读布局再写样式。
4. 上／下方向键移动真实选项焦点，Enter 选择，Escape 关闭并恢复输入框焦点。切换字段或手工输入时撤销过时请求。
5. 等待、失败和重试有可见反馈；选择后不能无提示地消失，也不能自动重复触发系统认证。

## 4. 工具栏刷新与状态

工具栏图标只有两种底色：**尚未成功连接并解锁，一律红底；验证成功、可以使用密码后才恢复白底。** 启动、重试、等待验证码、验证码错误、需要重新验证、断连和助手不可用均使用红底，保留原有黑色图案、形状和尺寸。仅原生助手端口连通或能力协商完成不算用户已成功连接，`needs_pin` 必须保持红底；只有 `unlocked` 使用白底。悬停文字提供中英文说明并区分各状态，待验证时不得宣称已连接成功。状态更新不得发起认证或凭据查询；异步图标绘制不得让旧未就绪状态覆盖成功解锁后的白底。

恢复原图时须使用扩展根目录的完整资源地址，不能把 `icons/…` 交给位于 `src/` 下的 worker 按相对路径解析。回归须在真实浏览器图标 API 上验证红底到白底的切换成功；仅模拟 `setIcon` 的调用不能作为资源加载验收。

1. **刷新只查询账号列表并使相关缓存失效**，不读取密码、不隐式填充、不主动重新发起六位码认证；需要授权时明确显示锁定状态。
2. 初次加载、刷新和填充互斥。查询中重复点击刷新合并／忽略，不积累后续任务。
3. **刷新期间保留账号列表及其位置**，禁止先清空再重建或反复切换整页视图。列表下方提示允许按实际内容展开／收起，不能为保持窗口高度而在无提示时预留空白。
4. 相同账号复用原条目节点；实际数据或顺序变化时才更新列表。相同状态通知不重复查询或重建界面。
5. 进度只在刷新图标和列表下方统一状态区域反馈。共用同一节点，不额外插入临时面板；没有提示时不保留高度或间距。
6. 刷新失败保留已有列表，在列表下方统一状态位置显示准确错误；首次失败也使用同一节点和样式。随后成功的空结果须恢复默认空状态，隐藏域名，不同时显示另一条刷新成功提示。所有文字提示（含设置说明、保存状态和网页浮层）统一字体、13px 字号、650 字重、1.45 行高和上下 8px 留白；普通提示使用正文色，影响使用的失败使用主题错误红色。
7. 账号查询只要求有效站点，不要求输入框或可用内容脚本。无法取得文档时可查看账号，但禁用填充；不得因此在刷新时报“页面或所选字段已变化”。
8. 实际填充仍验证文档与字段；真实导航、失效会话和字段变化不能绕过检查。“再次填充”是独立操作，保留原 frame／文档目标。

## 5. 字段与填充安全

1. 仅 HTTPS 和明确回环地址可以使用凭据；`.test`、私网 HTTP 和普通 HTTP 域名不默认视为安全例外。
2. 填充绑定 origin、tab、frame、documentId、内容脚本文档标识与请求生命周期；认证期间导航不能把结果带到新页面。
3. 每个字段写入前重查连接、类型、所属表单、可编辑性与可见性；前一字段的网页事件不能绕过后一字段的检查。
4. 排除隐藏、透明、完全裁剪、遮挡、disabled、readonly、inert 和禁用 fieldset 内字段。OTP 判断优先于 password 类型。
5. “显示密码”后的文本框依据密码语义识别；不能因此把普通文本框或验证码框当作密码框。
6. 跟随最近明确操作的登录字段，多表单有歧义时要求用户选择，不默认填第一组。返回实际填写字段和阶段，区分两步用户名登录与密码填写失败。
7. 新密码识别限定局部表单；生成器只填新密码和确认字段，不填当前密码。
8. 工具栏的“生成强密码”入口使用密码生成器的加密随机源，默认生成 16 位、不含特殊字符的密码，允许用户设置 8—64 位长度及是否包含特殊字符，展示一次性结果并提供明确的复制操作；聚焦新密码字段时，内联菜单默认同样生成 16 位字母数字密码，另提供包含特殊字符的选项，并直接填入新密码和确认字段。弹窗不显示重复的生成结果标题或解释 Copy 按钮的说明。
9. 保留 closed Shadow DOM、顶层 popover、宿主完整性与真实交互命中检查。closed 模式不是唯一安全边界，UI 改动不得撤掉安全检查。
10. 内容脚本会在普通 HTTP 页面启动，不得依赖仅安全上下文提供的 `crypto.randomUUID()`。文档、填充、生成及提交标识统一通过 `crypto.getRandomValues()` 产生 128 位随机值，不使用时间戳或 `Math.random()` 替代；启动兼容不扩大 HTTP 凭据访问范围。回归须在缺少 `randomUUID` 的环境中执行实际内容脚本，验证启动与文档隔离。

## 6. 会话、协议与缓存

1. PIN、密钥和密码只在必要的内存生命周期中保留，不写入日志、附件或持久化存储。六位码支持粘贴，成功后清空；新码请求、输入和校验互斥。
   六位码输入框与 Unlock 按钮必须在同一行；输入框只保留一层边框，聚焦通过边框颜色表示，不叠加第二层外框。验证码说明、请求进度、校验进度和错误只在输入区域下方统一状态节点更新，不在上下堆叠多个提示框。
   用户每次明确点击内联解锁，必须先请求新的验证码，再交接到弹窗；打开弹窗成功不得提前结束发码流程。关闭 macOS 验证码窗口不等于客户端已收到挑战失效通知，不能因为旧挑战未过期就跳过这次明确请求。弹窗初始化只复用刚才的挑战以避免重复发码；“获取新验证码”仍强制发起新挑战。
   “存在可用挑战”和“本次实际新发验证码”是不同结果；复用旧挑战时不得显示“新码已就绪”。弹窗接管输入后移除网页上的同类状态提示，失败时保留明确的重新请求入口。
2. 断连、InvalidSession 或重新登录通知须使会话失效、清缓存，并拒绝在途／排队操作。
3. 无关联 ID 的原生请求超时后销毁连接，迟到回复不得被下一请求接收。取消和排队等待须有期限。
4. 凭据身份使用 origin 与精确原始用户名，或稳定凭据 ID；大小写、空格、零宽字符不同的账号不得因展示去重而合并。
5. 密码缓存最长两分钟、账号缓存最长十五秒；读取时检查过期时间。会话代次变化后，旧查询不得重新写入缓存。
6. 同站点并发账号查询共享在途 Promise；保存请求发出后，使账号与密码缓存一起失效。
7. 严格验证编码、认证标签、帧长度、TID 和协议版本，拒绝非法输入与降级，不静默修复异常数据。

## 7. 保存与更新

1. 只有真实提交意图才能交接保存；输入框中的空格不是提交。优先 HTML 语义，再补本地化规则。
2. 快照限定实际提交的表单／局部字段，排除隐藏、禁用和无关数据，检查确认密码一致性。同步交给后台，再计算摘要，减少导航丢失窗口。
3. 已有账号也能提交更新；去重同时考虑站点、精确账号、密码版本和提交事件。
4. 待保存任务在排队及执行期间均受三分钟 TTL 限制；提供待解锁、失败、结果不确定、过期、重试和取消反馈。
5. 自动重试有次数限制；助手可能已收到的未确认操作不能盲目重发。“已交给助手”不等于密码已存入保险库。
6. 任务仅存在后台内存，worker／浏览器重启会丢失；取消本地任务不能撤回系统已经接收的操作。
7. 保存 cmd 6 按 Apple 官方客户端方式单向发送，不建立等待回复的 waiter，不用三秒、八秒或两分钟无回复超时主动断开连接，不占用后续查询队列。站点包含明确端口；普通登录使用对应的保存 QID。成功发送只表示“已发出请求，请在 Mac 上确认”，不表示助手确认或保险库已写入；投递异常报告结果不确定且不自动重发。三分钟 TTL 约束交接前的排队与加密阶段，不能在交接后通过断连关闭 Apple 确认框。

## 8. 浏览器控制、无障碍与本地化

1. 浏览器保存提示和地址自动填充开关由用户主动启用，不修改信用卡／付款自动填充设置。
2. 可选策略助手只操作当前浏览器，界面说明范围；区分策略控制来源与实际布尔值。
3. 可访问名称包含具体状态和账号，错误／进度可播报，不能只靠颜色和视觉选中态表达状态。
4. 中英文 UI 与错误共用消息目录。Shadow DOM 键盘、焦点和表单事件使用正确事件路径，填写事件跨根传播。

## 9. 变更验收

1. UI 修改覆盖浅／深色、少量／大量条目、视口边缘、键盘和缩放；本轮布局与刷新回归见 `test-harness/automation/drive-experience.mjs`。
2. 刷新至少覆盖解锁后首次加载、快速连点、多次完整刷新、慢查询、失败恢复、空列表和无内容脚本页面。
3. 浏览器测试运行生产后台，主要在原生传输边界使用虚构数据；安全回归保留 closed 模式，不以搜索源码字符串代替行为断言。
4. 运行相关静态检查、单元测试、浏览器回归与构建，不吞掉有意义的失败；临时 profile 由创建者在 finally 中清理。
5. 真实 Apple 助手、系统认证、保险库存储、VoiceOver 和不同系统／浏览器组合单独验收，自动化与人工结果分开记录。

## 10. Service Worker 加载与构建

1. 项目根目录和 dist 都必须使用同一份自包含 classic 后台脚本。根目录入口为 `src/background.bundle.js`，dist 为兼容已加载版本保留 `src/background.js`；二者内容必须一致。不得只修 dist，却让根目录退回模块加载入口。
2. 后台运行时不再分批加载源码依赖；模块源码保留在 `src/background.js` 等文件中，由构建工具统一打包。生成文件随项目保存，修改后台源码后运行 `npm run bundle:background` 或 `npm run build`。
3. `npm run check` 必须验证入口类型、生成文件存在且与源码一致，并验证产物可以作为单个 classic 脚本解析。缺失或过期时直接失败，不允许静默继续。
4. 构建必须先编译并准备所有资源，再通过同目录临时文件的原子重命名替换目标。不得先删除已加载目录、截断现有脚本或把半成品暴露给浏览器；manifest 最后发布。
5. 内容未变化时不重写文件、不修改时间戳。编译失败保留上一份可加载产物；重建当前版本不得删除其他可能仍被浏览器加载的版本目录。
6. 扩展重新加载期间，后台应保持同步注册监听器、异步连接助手。不能用重复 reload、忽略脚本加载错误或添加无意义的常驻操作掩盖启动失败。
7. 对 `An unknown error occurred when fetching the script`，先核对浏览器实际加载路径、manifest 入口、生成文件及依赖是否可用，不直接断言是刷新频率问题。无效的导入也会触发同类错误，见 [Chrome 官方调试说明](https://developer.chrome.com/docs/extensions/get-started/tutorial/service-worker-events#optional-debugging-the-service-worker)。
8. 回归须覆盖生成文件过期、构建失败、构建期间并发读取、重复构建不改文件。浏览器连续重新加载属于独立验收，不得把文件级测试通过当作真实浏览器问题已完全复现或验收。
